import 'package:flutter/material.dart';

class CardsListScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Cards List')),
      body: Center(
        child: Text('Cards List Screen'),
      ),
    );
  }
}
